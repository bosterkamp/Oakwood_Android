<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#000000"
    android:textSize="20sp">
    
<TextView
	android:id="@+id/sermon_selection_text"
	android:layout_width="fill_parent" 
    android:layout_height="wrap_content"
    android:text="@string/eray_selection_content"
    android:textColor="#FFFFFF"
    android:textSize="20sp"
    />
    
<ListView android:id="@+id/rssListView"
    android:layout_below="@id/sermon_selection_text" 
    android:layout_width="fill_parent" 
    android:layout_height="wrap_content"
    android:background="@null" 
    android:textColor="#FFFFFF"
    />  
    

<TextView 
    android:layout_width="wrap_content" 
    android:layout_height="wrap_content"
    android:layout_below="@id/rssListView" 
    android:id="@+id/errors"></TextView>
</RelativeLayout>